pub input data(address:u8) -> (byte:u8)

fn main() {
    var x:u8 = data[0]
    x = doubleCapped(x)
    if (x != data[1]) {
        fail
    }
}

fn doubleCapped(x:u8) -> (r:u8) {
    r = (x >= 128) ? 255:2 * x
    return
}
