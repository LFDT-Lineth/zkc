pub input data(address:u8) -> (byte:u8)

fn main() {
    var cond:u8 = data[0]
    var expected:u8 = data[1]
    var a:u8 = 1

    // skip_if amount 1 over ret: do not wrap.
    if cond == 0 {
        return
    }

    if a != expected {
        fail
    }
    return
}
