// this test performs multiple assignments to a cell of an mono-line address memory
// which violates its "write once" property; it should FAIL, but it doesn't ...
output WOM(address_1:u8) -> (value:u4)

fn main<>() {
    WOM[0] = 0
    WOM[0] = 1
    WOM[0] = 2
}
