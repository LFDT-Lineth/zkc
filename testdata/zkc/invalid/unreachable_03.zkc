//error:6:1-24:module "ping" unreachable via lookups from entry point "main"
//error:11:1-24:module "pong" unreachable via lookups from entry point "main"
pub input data(address:u16) -> (byte:u8)
pub output result(address:u16) -> (byte:u8)

fn ping(x:u8) -> (y:u8) {
    y = pong(x)
    return
}

fn pong(x:u8) -> (y:u8) {
    y = ping(x)
    return
}

fn main() {
    result[0] = data[0]
    return
}
